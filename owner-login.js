const sb=window.supabase.createClient(window.TNYPL_CONFIG.SUPABASE_URL,window.TNYPL_CONFIG.SUPABASE_ANON_KEY);
async function signInOwner(e){e.preventDefault();const email=document.getElementById("ownerEmail").value.trim().toLowerCase(),password=document.getElementById("ownerPassword").value,msg=document.getElementById("loginMessage");msg.textContent="Signing in…";const {data,error}=await sb.auth.signInWithPassword({email,password});if(error||!data.user){msg.textContent=error?.message||"Unable to sign in";return;}const {data:a,error:ae}=await sb.from("franchise_owner_assignments").select("franchise_slug,franchise_name,owner_name,is_active").eq("user_id",data.user.id).eq("is_active",true).maybeSingle();if(ae||!a){await sb.auth.signOut();msg.textContent="Login valid, but no active franchise is assigned to this account. Contact the TNYPL administrator.";return;}sessionStorage.setItem("tnypl_owner_assignment",JSON.stringify(a));location.href="owner-dashboard.html";}
document.getElementById("ownerLoginForm")?.addEventListener("submit",signInOwner);