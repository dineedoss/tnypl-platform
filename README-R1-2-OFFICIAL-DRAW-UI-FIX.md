# TNYPL R1.2 Official Draw UI Fix

Fixes:
- Franchise names now render inside the wheel.
- Adds an All 8 Franchises panel.
- Shows each franchise as Unassigned, Group A/B, or A1-B4.
- Shows draw ID, status, and franchise count.
- Prevents switching directly to position stages when no teams are assigned.
- Uses cache-busted JS/CSS URLs.

Deploy on the release branch, then hard-refresh `/official-draw-admin.html`.
